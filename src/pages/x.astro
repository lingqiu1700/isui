<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>平行布局示例</title>
    <style>
        /* 容器样式 */
        #navigation {
            display: flex;
            justify-content: center; /* 居中对齐 */
            gap: 20px; /* 按钮之间的间距 */
            background-color: lightgreen; /* 背景色，您可以自行调整 */
            padding: 10px; /* 添加一些内边距 */
        }

        /* 按钮样式 */
        .nav-item {
            display: inline-block;
            padding: 10px 20px;
            background-color: white; /* 按钮背景 */
            border-radius: 5px; /* 圆角 */
            text-decoration: none; /* 去掉下划线 */
            color: black; /* 字体颜色 */
            font-size: 16px;
            text-align: center;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2); /* 阴影效果 */
        }

        .nav-item:hover {
            background-color: #f0f0f0; /* 鼠标悬停效果 */
        }
    </style>
</head>
<body>
    <div id="navigation">
        <a href="index.html" class="nav-item">首页</a>
        <a href="about.html" class="nav-item">关于</a>
    </div>
</body>
</html>
